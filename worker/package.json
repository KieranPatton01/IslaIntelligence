{
  "name": "isla-intelligence-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker — secure Gemini 2.0 Flash API proxy with dynamic tone injection",
  "scripts": {
    "dev":    "wrangler dev",
    "deploy": "wrangler deploy",
    "tail":   "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
